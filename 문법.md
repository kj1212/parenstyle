# 적용 문법 — 인디자인에 스타일을 붙여 넣는 방법

이 프로그램이 무엇을 만들어서 어디에 얹는지, 그리고 **왜 그렇게 해야만 하는지**를
적어 둔다. 여기 적힌 함정은 전부 실제로 당해 본 것이다.

---

## 1. 큰 그림

```
원고 평문
   │
   ├─ 괄호 찾기 → 갈래 가르기(한자/숫자/영문/한글/나머지)
   │
   ├─→ RTF 짓기 (스타일 이름이 들어간다)  ─┐
   └─→ 괄호만 벗긴 평문                   ─┤
                                          ↓
                            클립보드에 서랍 둘을 함께 얹는다
                                          ↓
                                 인디자인이 RTF 서랍을 연다
                                          ↓
                        스타일시트의 **이름**으로 문서의 기존 스타일을 찾아 붙인다
```

핵심은 **스타일 이름**이다. RTF는 서식(굵게·윗첨자)뿐 아니라 **스타일 이름**을 실어 나를 수
있고, 인디자인은 그 이름으로 문서에 이미 있는 스타일을 찾아 연결한다. 실측으로 확인했다 —
새 스타일을 만들지 않고 기존 것에 정확히 붙었다.

---

## 2. RTF 구조

만들어 내는 것의 전체 모양이다. (한글은 실제로는 `\uN ?` 로 escape 되지만 여기서는 읽기 쉽게 풀어 적는다)

```rtf
{\rtf1\mac\ansicpg949\deff0\deftab720
{\fonttbl{\f0\fnil\ftnil\cpg10003\fcharset79 AdobeMyungjoStd-Medium;}}
{\colortbl\red0\blue0\green0;}
{\stylesheet{\s0\snext0 [기본 단락];}{\*\cs1 \additive \super 한자윗첨자;}{\*\cs2 \additive \super 윗첨자;}}
\ksulang1041
\pard\plain \s0 공자{\cs1\super 孔子}는 말했다{\cs2\super 주석1}.\par
\pard\plain \s0 괄호 없는 줄.\par
}
```

### 머리말

`\rtf1\mac\ansicpg949` — 인디자인이 **자기가 뱉는** RTF의 머리말을 그대로 본떴다.
`\fonttbl`·`\colortbl`·`\ksulang1041` 도 인디자인 것을 따랐다.
(인디자인이 실제로 뱉은 것을 클립보드에서 떠서 베꼈다)

### 스타일시트 — 여기가 이름이 들어가는 곳

| 조각 | 뜻 |
|---|---|
| `{\s0\snext0 [기본 단락];}` | **단락** 스타일 0번의 이름은 `[기본 단락]` |
| `{\*\cs1 \additive \super 윗첨자;}` | **문자** 스타일 1번의 이름은 `윗첨자` |

- 이름은 **세미콜론 앞**에 온다.
- `\additive` = 밑에 깔린 서식 위에 얹는다 (문자 스타일의 정상 동작)
- `\super` = 윗첨자. **이름 매칭이 실패했을 때의 보험**이다. 이름이 안 맞아 스타일이
  안 붙어도 최소한 윗첨자로는 보인다.
- 스타일 이름은 인디자인 문서에 있는 것과 **글자 하나까지 같아야** 한다.
  다르면 인디자인이 그 이름으로 **새 스타일을 만들어버린다.**
- 내장 단락 스타일의 한국어판 이름은 대괄호에 띄어쓰기가 있다: **`[기본 단락]`**
  (영문판은 `[Basic Paragraph]`, `[단락 스타일 없음]` = `[No Paragraph Style]`)

### 본문

```
\pard\plain \s0 앞글자{\cs1\super 안쪽글자}뒷글자\par
```

- `\pard\plain` = 단락 서식 초기화
- `\s0` = 이 단락에 단락 스타일 0번을 물린다. **안 붙이면** 붙여넣는 자리의
  단락 스타일을 그대로 물려받는다 (창의 "단락 스타일" 체크를 끄면 이렇게 된다)
- `{\cs1\super ...}` = 이 중괄호 안쪽만 문자 스타일 1번
- `\par` = 단락 끝

### ★★ 절대 하지 말 것 — 본문에서 `\*` 쓰기

```rtf
{\*\cs1 안쪽글자}     ← 글자가 통째로 사라진다
{\cs1\super 안쪽글자} ← 이게 맞다
```

`\*` 는 RTF에서 **"모르는 것이면 이 덩어리를 통째로 버려라"** 라는 뜻이다.
스타일시트 **정의부**에서는 `{\*\cs1 ...;}` 가 맞지만, **본문 참조부**에 쓰면
읽는 쪽이 글자까지 삼켜버린다. 처음에 이걸로 헤맸다.

(인디자인 자신은 본문에도 `\*\cs2` 를 쓰는 것처럼 보이지만, 따라 하지 말 것)

### 한글 escape

RTF는 7비트 아스키가 안전하다. 아스키 밖 글자는 전부 이렇게 바꾼다:

```
'윗'(U+C717 = 50967)  →  \u-14569 ?
```

- 16비트 **부호 있는** 수다. 32767보다 크면 65536을 뺀다.
- 뒤의 ` ?` 는 유니코드를 못 읽는 옛 읽개용 대체 글자. **반드시 있어야 한다.**
- BMP 밖(U+10000 이상)은 서로게이트 쌍 두 개로 쪼갠다.
- 이 덕분에 **결과 RTF는 순수 아스키**가 되어 인코딩 사고가 없다.
  (`engine.rtf_escape()`)

---

## 3. 클립보드에 얹기

클립보드는 한 덩어리가 아니라 **같은 내용을 여러 서랍에 따로** 담는 구조다.
받는 쪽이 서랍 하나를 골라 연다. 그래서 RTF 서랍과 평문 서랍을 **둘 다** 채운다.

### 맥

`osascript -l JavaScript` 로 JXA를 돌려 `NSPasteboard` 에 얹는다.
(파이썬에서 AppKit을 직접 부르려면 pyobjc가 필요한데, 딸린 것 없이 가려고 이렇게 했다)

```
declareTypes:owner:  로 형식을 먼저 선언한 뒤 setData:forType:
  public.rtf
  NeXT Rich Text Format v1.0 pasteboard type   ← 옛 이름. 같이 얹는다
  public.utf8-plain-text
  NSStringPboardType
```

### Windows

`ctypes` 로 Win32 API를 직접 부른다. 딸린 것 없다.

```
CF_RTF = RegisterClipboardFormatW("Rich Text Format")   ← 등록해서 번호를 받는다
CF_UNICODETEXT = 13

OpenClipboard(NULL) → EmptyClipboard()
  → GlobalAlloc(GMEM_MOVEABLE, 길이) → GlobalLock → memmove → GlobalUnlock
  → SetClipboardData(형식, 핸들)
→ CloseClipboard()
```

주의할 점:
- RTF는 **바이트**로 넣는다 (우리 RTF는 순수 아스키). 끝에 NUL 하나.
- 평문은 **UTF-16LE** + NUL 둘.
- `SetClipboardData` 가 **성공하면 메모리 소유권이 시스템으로 넘어간다.**
  거기서 `GlobalFree` 하면 안 된다. 실패했을 때만 푼다.
- 줄바꿈은 `\r\n` 으로 바꿔서 넣는다.
- 64비트에서 핸들이 잘리지 않게 `argtypes`/`restype` 를 반드시 지정한다.

**★ 이 Windows 경로는 아직 실측을 못 했다.** 맥에서 짰다. 처음 돌릴 때 확인할 것.
`자가진단.py` 를 돌리면 얹고 되읽어서 맞는지 봐 준다.

---

## 4. ★★ 가장 큰 함정 — 인디자인 설정

**인디자인 > 설정(환경설정) > 클립보드 처리**
→ "다른 응용 프로그램의 텍스트 및 표 붙여넣기" = **"모든 정보"**

여기가 **"텍스트만"이면 인디자인이 RTF 서랍을 열어보지도 않는다.**
평문 서랍만 열기 때문에 겉으로는 "붙여넣기가 안 된다 / 그냥 글씨만 나온다"로 보이고,
**코드가 틀린 줄 착각하게 된다.** 실제로 한참 헤맸다.

스크립트로 읽는 이름: `app.clipboardPreferences.preferStyledTextWhenPasting`
(맥·Windows 공통. 스크립트로 켜도 안 붙잡히니 UI에서 켤 것)

증상이 이러면 **여기부터** 볼 것.

---

## 5. 안 되는 길 — 왜 접었나

### 인디자인 내부 붙여넣기 흉내내기 → 불가

인디자인끼리의 붙여넣기를 클립보드에서 떠 보면:

```
     0  com.adobe.indesign-interchange                      ← 비어 있다
     8  com.adobe.indesign.local-private-clipboard-marker   0x80d9f81807000000
```

전용 형식이 **0바이트**다. 진짜 데이터는 클립보드에 없고 **인디자인 제 프로세스
메모리**에 있으며, 클립보드엔 "이거 내 거야" 하는 8바이트 표딱지만 얹힌다.
바깥에서 흉내낼 대상 자체가 없다.

### 웹앱 → 불가

브라우저 클립보드는 넣을 수 있는 형식이 안전 목록(평문·HTML·PNG)으로 제한된다.
RTF는 목록에 없고, 커스텀 형식은 `web ` 접두사가 강제로 붙어서
인디자인 같은 native 앱은 그 서랍을 아예 못 본다.

---

## 6. 안 쓴 길 — 나중에 필요하면

### `com.adobe.cc-text-interchange` (순수 JSON)

인디자인이 복사할 때 이 서랍도 채운다. **읽을 수 있는 JSON**이고 RTF보다 정확하다.

```json
{
  "adbeIdsnTextDirection": "HORIZONTAL_TYPE",
  "paragraphs": [
    { "text": "기본단락윗첨자",
      "styles": [125],
      "runs": [ { "from": 4, "to": 7, "styles": [282] } ] }
  ],
  "styles": {
    "125": { "name": "[Basic Paragraph]",
             "type": "application/vnd.adobe.paragraphstyle+json", "value": {...} },
    "282": { "name": "윗첨자",
             "type": "application/vnd.adobe.characterstyle+json",
             "value": { "fontFeatureSettingsObject": { "adbePosition": "Position.SUPERSCRIPT" } } }
  }
}
```

- 글자 위치를 `from`/`to` 로 정확히 집는다 (RTF의 중괄호 중첩보다 다루기 쉽다)
- 스타일 **이름**을 들고 있다
- 끝에 NUL 한 바이트가 붙어 있으니 파싱 전에 떼야 한다
- 문서화된 적 없는 형식이다. **인디자인이 이걸 읽어서 붙여넣는지는 확인 안 했다**
  (쓰는 것만 봤다). RTF로 충분해서 더 안 팠다.

### ICML / IDML

`.icml`(InCopy 마크업)은 스타일까지 온전히 실어 나르는 **공식** 형식이다.
다만 클립보드가 아니라 **파일 > 가져오기(⌘D)** 로 들어간다.
클립보드가 끝내 막히면 여기로 간다.

### 인디자인 안에서 도는 판 (GREP)

맥 스크립트 패널에 `괄호를 문자스타일로.jsx` 를 깔아 뒀다.
GREP 찾기/바꾸기로 문서 안에서 직접 처리한다. 클립보드를 안 거치니
위 함정이 하나도 없다. Windows에도 옮기려면 스크립트 패널 폴더에 넣으면 된다:

```
%APPDATA%\Adobe\InDesign\Version 21.0-J\ko_KR\Scripts\Scripts Panel\
```

찾을 GREP: `[(（]([^()（）]+)[)）]` → 바꿀 내용 `$1` + 문자 스타일 지정

---

## 7. 맥 tkinter 함정 (Windows에선 해당 없음)

맥 기본 파이썬(Xcode CLT 3.9)은 **Tk 8.5.9** 라서 요즘 macOS에서
**창을 빈 화면으로 그린다.** 홈브루 파이썬(Tk 9.0.3)으로 띄워야 한다.
`실행-맥.command` 가 알아서 골라 준다.

Windows의 python.org 판은 Tk 8.6 이 딸려 오므로 그냥 된다.
