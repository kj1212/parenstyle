# parenstyle — 이어서 작업할 때 읽을 것

인디자인 책편집용 도구. 원고의 `(괄호)`를 벗기고, RTF 를 만들어 클립보드에 담는다.
사용자는 인디자인에 그대로 붙여넣는다. 인디자인이 실제로 쓰는 방식 그대로 물린다:
- **문단** → 단락 스타일 (`\sN`)
- **본문 글자(괄호 밖)** → 본문 문자 스타일 (`{\csN ...}`)
- **괄호 안** → 위첨자 문자 스타일 (`{\csN\super ...}`), 같은 문단 안 인라인
  (인라인이라 단락 스타일은 본문과 공유. 위첨자는 문자 스타일만 다르다.)

만든이는 인디자인으로 책을 편집하는 사람이다. 프로그래머가 아니다.
**창(GUI)이 주 사용 방법**이고 터미널은 진단용이다.

## 지금 상태 (2026-08-24)

| | |
|---|---|
| 맥 | **실측 완료.** 자가진단 통과, 인디자인 붙여넣기까지 확인 |
| Windows | **실측 완료.** 자가진단 14/14 통과 + 인디자인 붙여넣기 확인(2026-08-24). 본문 단락+문자 / 괄호 위첨자 정상 |

**본문 문자 스타일**(괄호 밖 글자에 문자 스타일을 물리는 것)은 나중에 추가됨. 그전엔
본문에 단락 스타일만 물렸다. GUI 1단계에 '단락 스타일'·'문자 스타일' 두 칸이 있고,
`engine.build_rtf(text, rules, para_style_name, body_char_style, keep_parens)` 로 넘긴다.

Windows 에서 처음 할 일:

```
python 자가진단.py
```

`[4] 클립보드` 가 통과하면 Win32 경로가 맞는 것이다.
실패하면 `문법.md` 3장 "Windows" 를 볼 것 — 핸들 소유권·argtypes 함정을 적어 뒀다.

## 실행

| 맥 | `실행-맥.command` |
| Windows | `인디자인_실행-윈도우.bat` |

맥은 **반드시** 이 launcher 로. 기본 파이썬은 Tk 8.5.9 라서 **창이 빈 화면으로 뜬다**
(실제로 당했다). launcher 가 Tk 8.6+ 인 파이썬을 찾아 준다.

## 파일

| 파일 | |
|---|---|
| `gui.pyw` | 창. tkinter, 딸린 것 없음 |
| `engine.py` | 괄호 찾기 · 갈래 가르기 · RTF 짓기 |
| `clipboard_rtf.py` | 클립보드 얹기/읽기. 맥 = JXA+NSPasteboard, Windows = ctypes+Win32 |
| `자가진단.py` | 옮겨온 곳에서 도는지 검사 |
| `문법.md` | **RTF 문법과 함정 전부.** 손대기 전에 읽을 것 |
| `README.md` | 사용자용 설명 |

## 손대기 전에 알아야 할 세 가지

`문법.md` 에 자세히 있다. 요약만:

1. **본문에서 `{\*\cs1 ...}` 로 쓰면 글자가 통째로 사라진다.** `{\cs1\super ...}` 가 맞다.
   `\*` 는 스타일시트 **정의부**에만 쓴다.
2. **인디자인 설정 > 클립보드 처리 = "모든 정보"** 가 아니면 RTF 서랍을 열지도 않는다.
   "붙여넣기가 안 된다"는 신고가 오면 **코드보다 여기부터** 의심할 것. 한참 헤맸다.
3. **RTF 결과는 순수 아스키를 유지한다.** 한글은 `\uN ?` 로 escape (`engine.rtf_escape`).
   인코딩 사고를 원천 차단하는 장치이니 깨뜨리지 말 것.

## 코드 관례

- 딸린 라이브러리를 **늘리지 않는다.** 표준 라이브러리만. 사용자가 두 대의 기계에
  폴더째 복사해 쓰기 때문이다.
- 파이썬 3.9 에서도 돌게 쓴다 (맥 기본 파이썬이 3.9다. 창은 못 띄워도
  `engine.py`·`자가진단.py` 는 돌아야 한다).
- 주석과 화면 글은 **한국어**, 평이한 말로. 되도록 외래어 대신 우리말.
- 기능을 더할 때 `자가진단.py` 에 검사 항목도 같이 더한다.

## 아직 안 한 것

- `.exe` 로 묶기 (PyInstaller). 파이썬 설치 없이 쓰게 하려면
- 대괄호 `[]`·홑화살괄호 `〈〉` 등 다른 묶음표
- 중첩 괄호 (지금은 안 다룬다)
- 인디자인이 `com.adobe.cc-text-interchange`(JSON) 를 **읽기**도 하는지 (`문법.md` 6장).
  RTF 로 충분해서 안 팠다. RTF 가 막히면 여기가 다음 수.
